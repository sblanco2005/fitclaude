import { ComingSoon } from '@/components/redesign/ComingSoon';

// Screen 03 · Hit It (live workout) — full build pending. A session Workout has
// already been created (duplicated) at this point via POST /api/workouts/{id}/duplicate.
export default function Page() {
  return <ComingSoon screen="Hit It" accent="var(--rd-ember)" />;
}
